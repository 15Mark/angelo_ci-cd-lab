apiVersion: tekton.dev/v1beta1
kind: Task
metadata:
  name: cleanup-task
spec:
  steps:
    - name: cleanup
      image: bitnami/kubectl:latest
      script: |
        #!/bin/sh
        echo "Cleaning up resources..."
        kubectl delete pod -l app=my-app || echo "No pods to deletion"
